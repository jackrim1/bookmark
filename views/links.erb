<h1>My Links</h1>

<ul id='links'>
  <% @links.each do |link| %>
    <li>
      Title: <%= link.title %>,
      URL: <%= link.url %>,
      Tags:

     <% link.tags.each do |tag| %>
     	<a href="/tags/<%= tag.name%>"> <%= tag.name %><a>,
     <%end%>
    </li>
  <% end %>
</ul>